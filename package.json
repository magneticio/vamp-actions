{
  "name": "node12-template-action",
  "version": "0.0.0",
  "private": true,
  "description": "Node 12 template action",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "clean": "del **/lib/ **/src/**/*.d.ts **/src/**/*.js tsconfig.tsbuildinfo **/tsconfig.tsbuildinfo !node_modules/**/*",
    "ci": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/actions/node12-template.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "author": "GitHub",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/io": "^1.0.0",
    "@actions/tool-cache": "^1.1.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.7.2",
    "del-cli": "^2.0.0",
    "jest": "^24.8.0",
    "jest-circus": "^24.9.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}
